plugins {
    id 'java-library'
    id 'maven-publish'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId group
            artifactId 'amf-vocabulary'
            version '1.5'

            from components.java
        }
    }
}
