plugins {
    id 'java-library'
    id 'maven-publish'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId group
            artifactId 'amf-vocabulary'
            version '1.0.0'

            from components.java
        }
    }
}
